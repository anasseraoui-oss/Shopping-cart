# Copy this file to jenkins.local.env and fill in your values.
# jenkins.local.env is gitignored — never commit real tokens.

JENKINS_URL="http://localhost:8082"
JENKINS_USER="admin"
JENKINS_TOKEN="your-api-token-here"
JOB_NAME="pipeline"
